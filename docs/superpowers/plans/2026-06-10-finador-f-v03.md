# Finador phase F — v0.3 : plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Retours utilisateur sur la vue web : (1) liens stylés au thème (fini le bleu navigateur) ; (2) édition des transactions au web (la CLI sait déjà : `tx edit`) ; (3) répartition en TROIS modes — par groupe, par enveloppe, par actif (liste plate) — avec arborescence dépliable à 3 niveaux (enveloppe → groupes → actifs, et réciproquement groupe → enveloppes → actifs), chaque niveau cliquable vers une vue de portée avec perfs et courbes, y compris les niveaux CROISÉS (enveloppe ∩ groupe).

**Architecture:** zéro JavaScript maintenu — l'arborescence dépliable utilise `<details>/<summary>` natifs stylés au thème. `portfolio` gagne `Breakdown` (la valeur de CHAQUE position et du cash, matière première des arbres) et la portée d'intersection `ByAccountGroup` (enveloppe ∩ groupe) dont Value/Series/perf découlent automatiquement via le chokepoint hasAsset. Routes web `/account/{ref}/group/{path...}` et `/group/{path}/account/{ref}` (même handler). Édition de transaction : `GET /tx/{id}/edit` (bordereau prérempli) + `POST /tx/{id}/edit`.

**Conventions inchangées** (TDD, gofmt/vet silencieux, français, pas de binaire committé, tests sans réseau, mutations web sous Lock + Save + 303, contrat de design « journal du patrimoine »).

**Décisions actées phase F :**
- L'arbre de répartition agrège les groupes au PREMIER segment (top-level) au niveau 2 ; les actifs au niveau 3. Le cash d'une enveloppe apparaît comme feuille « liquidités » (non cliquable) sous l'enveloppe ; en mode groupe, les liquidités forment une racine « liquidités » avec les enveloppes en enfants (pas de niveau actif).
- Le mode « par actif » est une liste PLATE : un actif agrégé sur toutes ses enveloppes, trié par valeur décroissante, cliquable vers `/asset/{id}`.
- La portée d'intersection n'est PAS exposée à la CLI en v0.3 (besoin web ; la CLI a déjà groupe/enveloppe/actif + --exclude).
- L'édition web d'une transaction CONSERVE son ImportHash (rééditer une ligne importée ne la fait pas revenir au prochain ré-import : voulu).
- `<details>` fermés par défaut (vue compacte) ; le lien du libellé navigue, le reste du `<summary>` déplie.

---

### Task F1: web — liens et arborescence au thème (CSS pur)

**Files:**
- Modify: `internal/web/static/style.css`
- Test: `internal/web/server_test.go` (ajout léger)

- [ ] **Step 1: test qui échoue**

```go
func TestStylesheetThemesLinksAndTrees(t *testing.T) {
	srv, _ := testServer(t)
	_, css := get(t, srv, "/style.css")
	for _, want := range []string{"main a {", "details", "summary", "--garance"} {
		if !strings.Contains(css, want) {
			t.Errorf("style.css: %q manquant", want)
		}
	}
}
```

- [ ] **Step 2: vérifier l'échec** — FAIL (pas de règle `main a {`).

- [ ] **Step 3: implémenter**

Ajouter à `internal/web/static/style.css` (section « liens » après les tables, et
section « arbre » après .repartition — le HTML arrive en F4 mais le style est prêt) :

```css
/* ---- liens : encre, jamais le bleu navigateur ---- */
main a {
  color: var(--encre);
  text-decoration: underline;
  text-decoration-color: var(--filet);
  text-decoration-thickness: 1px;
  text-underline-offset: .22em;
}
main a:hover { color: var(--garance); text-decoration-color: var(--garance); }
main a:visited { color: var(--encre); }

/* ---- arbre de répartition : details/summary au thème ---- */
.arbre, .arbre ul { list-style: none; margin: 0; padding: 0; }
.arbre ul { margin-left: 1.1rem; border-left: 1px solid var(--filet); padding-left: .7rem; }
.arbre li { padding: .12rem 0; }
.arbre details > summary {
  cursor: pointer; list-style: none; display: flex; align-items: baseline; gap: .6rem;
}
.arbre details > summary::-webkit-details-marker { display: none; }
.arbre details > summary::before {
  content: "▸"; color: var(--encre-2); font-size: .7rem; transition: transform .15s ease;
}
.arbre details[open] > summary::before { transform: rotate(90deg); }
.arbre .feuille { display: flex; align-items: baseline; gap: .6rem; padding-left: .95rem; }
.arbre .leader { flex: 1; border-bottom: 2px dotted var(--filet); transform: translateY(-4px); }
.arbre .montant { font-family: var(--mono); font-variant-numeric: tabular-nums; }
.onglets { display: flex; gap: .8rem; font-size: .8rem; margin-bottom: .5rem; }
.onglets .actif-onglet { font-weight: 600; border-bottom: 2px solid var(--encre); }
@media (prefers-reduced-motion: reduce) { .arbre details > summary::before { transition: none; } }
```

- [ ] **Step 4: vérifier** — `go test ./internal/web/ -count=1` vert ; lancer 3 s le
  serveur réel sur un livre temporaire et vérifier au curl que la CSS sert ces règles.

- [ ] **Step 5: commit** — `git add internal/web && git commit -m "style(web): liens à l'encre du thème, arborescence details/summary prête"`

---

### Task F2: portfolio — Breakdown et portée d'intersection

**Files:**
- Create: `internal/portfolio/breakdown.go`
- Modify: `internal/portfolio/scope.go` (ByAccountGroup, IntersectScope)
- Test: `internal/portfolio/breakdown_test.go` (créer), `internal/portfolio/scope_test.go` (ajout)

- [ ] **Step 1: tests qui échouent**

`internal/portfolio/breakdown_test.go` :

```go
package portfolio

import (
	"testing"

	"finador/internal/domain"
)

func TestBreakdownSumsToValue(t *testing.T) {
	b := valuationBook(t)
	at := mustDate("2026-06-05")
	lines, err := Breakdown(b, at, domain.EUR, fxStub{})
	if err != nil {
		t.Fatal(err)
	}
	total := 0.0
	for _, l := range lines {
		total += l.Gross
	}
	want, err := Value(b, scopeOf(t, b, ""), at, domain.EUR, fxStub{})
	if err != nil {
		t.Fatal(err)
	}
	approx(t, "Σ breakdown = Value", total, want.Gross)
	// chaque position porte compte ET actif ; le cash a Asset nil
	var cashLines, positions int
	for _, l := range lines {
		if l.Account == nil {
			t.Fatalf("ligne sans compte: %+v", l)
		}
		if l.Asset == nil {
			cashLines++
		} else {
			positions++
		}
	}
	if cashLines != 2 { // pea et livret sont suivis ; cto non
		t.Errorf("cashLines = %d, attendu 2", cashLines)
	}
	if positions != 3 { // pea/cw8, cto/cw8, immo/maison
		t.Errorf("positions = %d, attendu 3", positions)
	}
}

func TestIntersectScope(t *testing.T) {
	b := valuationBook(t)
	pea, _ := b.Account("pea")
	s := IntersectScope(pea, "actions")
	at := mustDate("2026-06-05")
	v, err := Value(b, s, at, domain.EUR, fxStub{})
	if err != nil {
		t.Fatal(err)
	}
	// seulement la position cw8 du PEA : 12 × 560 — ni le cash, ni le cw8 du CTO
	approx(t, "gross pea∩actions", v.Gross, 6720)
	// les séries fonctionnent sur la même portée (perfs/courbes des vues croisées)
	res, err := Series(b, s, mustDate("2026-01-01"), at, domain.EUR, fxStub{})
	if err != nil {
		t.Fatal(err)
	}
	last := res.Points[len(res.Points)-1]
	approx(t, "série pea∩actions", last.Gross, 6720)
}
```

- [ ] **Step 2: vérifier l'échec** — FAIL (Breakdown, IntersectScope inconnus).

- [ ] **Step 3: implémenter**

`internal/portfolio/scope.go` — la nouvelle sorte :

```go
const (
	All ScopeKind = iota
	ByGroup
	ByAccount
	ByAsset
	ByAccountGroup // intersection enveloppe ∩ groupe (niveaux croisés des arbres web)
)

// IntersectScope is the crossed level of the web trees: the assets of one
// group held inside one envelope. Cash is excluded (it has no group).
func IntersectScope(acc *domain.Account, group string) Scope {
	g := strings.ToLower(group)
	return Scope{Kind: ByAccountGroup, Account: acc, Group: g, Label: acc.Name + " › " + g}
}
```

Dans hasAsset (après le filtre Excluded) :

```go
	case ByAccountGroup:
		return acc.ID == s.Account.ID && inGroup(asset.Group, s.Group)
```

hasCash : ByAccountGroup n'inclut JAMAIS le cash (rien à ajouter : le switch ne le
liste pas). lineLabel : ajouter `case ByAccountGroup: return asset.Name` (asset nil
impossible : pas de cash dans cette portée — garder le garde-fou « liquidités » en
tête de fonction).

`internal/portfolio/breakdown.go` :

```go
package portfolio

import "finador/internal/domain"

// PositionLine is one valued position — or one envelope's cash when Asset is
// nil. The raw material of the web's hierarchical répartition trees.
type PositionLine struct {
	Account *domain.Account
	Asset   *domain.Asset
	Gross   float64
}

// Breakdown values every security position, property and tracked cash at
// `at`, in the display currency. Σ Gross equals Value(All).Gross.
func Breakdown(b *domain.Book, at domain.Date, ccy domain.Currency, fx FX) ([]PositionLine, error) {
	v := &valuer{b: b, fx: fx, at: at, ccy: ccy}
	var out []PositionLine
	for _, h := range Holdings(b, at) {
		if h.Asset.Kind == domain.Property {
			continue // valorisés par relevés ci-dessous
		}
		gross, err := v.positionValue(h)
		if err != nil {
			return nil, err
		}
		out = append(out, PositionLine{Account: h.Account, Asset: h.Asset, Gross: gross})
	}
	for _, p := range statementPairs(b, at) {
		if p.asset.Kind != domain.Property {
			continue
		}
		gross, err := v.statementValue(p.account.ID, p.asset)
		if err != nil {
			return nil, err
		}
		out = append(out, PositionLine{Account: p.account, Asset: p.asset, Gross: gross})
	}
	for _, acc := range b.Accounts {
		if !CashTracked(b, acc.ID) {
			continue
		}
		gross, err := v.cashValue(acc)
		if err != nil {
			return nil, err
		}
		if gross != 0 {
			out = append(out, PositionLine{Account: acc, Gross: gross})
		}
	}
	return out, nil
}
```

- [ ] **Step 4: vérifier** — `go test ./... -count=1` vert (le chokepoint hasAsset
  garantit que Series/perf héritent de l'intersection sans autre code).

- [ ] **Step 5: commit** — `git add internal/portfolio && git commit -m "feat(portfolio): Breakdown par position et portée d'intersection enveloppe∩groupe"`

---

### Task F3: web — vues de portée croisée /account/{ref}/group/{path}

URL canonique UNIQUE : `/account/{ref}/group/{gpath...}` (un motif Go ne peut pas
porter un segment multiple au milieu, donc pas de forme réciproque — les deux sens
de l'arbre pointent vers cette forme).

**Files:**
- Modify: `internal/web/server.go` (route), `internal/web/scope.go` (handler + refactor)
- Test: `internal/web/server_test.go` (ajout)

- [ ] **Step 1: test qui échoue**

```go
func TestIntersectionScopeView(t *testing.T) {
	srv, _ := testServer(t)
	code, body := get(t, srv, "/account/pea/group/actions")
	if code != http.StatusOK {
		t.Fatalf("GET intersection = %d\n%s", code, excerpt(body))
	}
	for _, want := range []string{"PEA Zephyr", "actions", "Amundi MSCI World", "performance", "<svg"} {
		if !strings.Contains(body, want) {
			t.Errorf("intersection: %q manquant", want)
		}
	}
	// le cash de l'enveloppe n'apparaît PAS dans une portée croisée
	if strings.Contains(body, "liquidités") {
		t.Error("liquidités présentes dans une intersection enveloppe∩groupe")
	}
	// compte inconnu → 404
	if code, _ := get(t, srv, "/account/zz9/group/actions"); code != http.StatusNotFound {
		t.Errorf("intersection inconnue = %d", code)
	}
}
```

- [ ] **Step 2: vérifier l'échec** — FAIL (404).

- [ ] **Step 3: implémenter**

Route (AVANT `GET /account/{ref}` dans le mux — les motifs Go 1.22 préfèrent le plus
spécifique, mais l'ordre lisible aide) :

```go
	mux.HandleFunc("GET /account/{ref}/group/{gpath...}", s.intersectPage)
```

Dans `internal/web/scope.go`, extraire de scopePage un
`renderScope(w *http.ResponseWriter…, scope portfolio.Scope)` réutilisé tel quel
(valeur --net du scope, courbe, perf.Report, transactions récentes), puis :

```go
func (s *Server) intersectPage(w http.ResponseWriter, r *http.Request) {
	s.mu.RLock()
	defer s.mu.RUnlock()
	b := s.file.Book
	acc, err := b.Account(r.PathValue("ref"))
	if err != nil {
		s.renderError(w, http.StatusNotFound, "compte introuvable : "+r.PathValue("ref"))
		return
	}
	scope := portfolio.IntersectScope(acc, r.PathValue("gpath"))
	s.renderScope(w, scope)
}
```

(scopePage devient : ParseScope puis `s.renderScope(w, scope)` — même rendu,
template scope.html inchangé ; le fil d'ariane affiche scope.Label qui contient
déjà « Compte › groupe ».)

- [ ] **Step 4: vérifier** — `go test ./internal/web/ -count=1 && go test ./... -count=1` vert.

- [ ] **Step 5: commit** — `git add internal/web && git commit -m "feat(web): vues croisées enveloppe∩groupe — /account/x/group/y"`

---

### Task F4: web — répartition en trois modes, arborescence dépliable

**Files:**
- Create: `internal/web/tree.go`
- Modify: `internal/web/handlers.go`, `internal/web/templates/dashboard.html`
- Test: `internal/web/tree_test.go` (créer), `internal/web/server_test.go` (ajout)

Modes (`?par=` ; défaut groupe) :
- **groupe** : groupe de tête → enveloppes (lien intersection) → actifs (lien
  /asset/x). Racine « liquidités » (non cliquable) avec les enveloppes en enfants.
- **enveloppe** : enveloppe (lien /account/x) → groupes de tête (lien intersection)
  → actifs ; le cash de l'enveloppe = feuille « liquidités » non cliquable.
- **actif** : liste plate agrégée par actif (toutes enveloppes), triée par valeur
  décroissante, lien /asset/x.
Tout niveau trié par valeur décroissante (déterministe). Les URLs utilisent les ID
(stables), les libellés les noms.

- [ ] **Step 1: tests qui échouent**

`internal/web/tree_test.go` :

```go
package web

import (
	"testing"

	"finador/internal/domain"
	"finador/internal/portfolio"
)

func sampleLines(t *testing.T) []portfolio.PositionLine {
	t.Helper()
	pea := &domain.Account{ID: "pea", Name: "PEA"}
	cto := &domain.Account{ID: "cto", Name: "CTO"}
	cw8 := &domain.Asset{ID: "cw8", Name: "CW8", Group: "actions/monde"}
	aapl := &domain.Asset{ID: "aapl", Name: "Apple", Group: "actions/us"}
	maison := &domain.Asset{ID: "maison", Name: "Maison", Group: "immo"}
	return []portfolio.PositionLine{
		{Account: pea, Asset: cw8, Gross: 6720},
		{Account: cto, Asset: cw8, Gross: 1120},
		{Account: cto, Asset: aapl, Gross: 2000},
		{Account: pea, Asset: maison, Gross: 450000},
		{Account: pea, Gross: 4050}, // cash
	}
}

func TestBuildTreeByAccount(t *testing.T) {
	tree := buildTree(sampleLines(t), "enveloppe")
	if len(tree) != 2 || tree[0].Label != "PEA" { // trié décroissant : PEA d'abord
		t.Fatalf("racines = %+v", tree)
	}
	pea := tree[0]
	if pea.URL != "/account/pea" {
		t.Errorf("URL pea = %q", pea.URL)
	}
	// enfants du PEA : immo (450000), actions (6720), liquidités (4050)
	if len(pea.Children) != 3 || pea.Children[0].Label != "immo" {
		t.Fatalf("enfants pea = %+v", pea.Children)
	}
	if pea.Children[1].URL != "/account/pea/group/actions" {
		t.Errorf("URL intersection = %q", pea.Children[1].URL)
	}
	if last := pea.Children[2]; last.Label != "liquidités" || last.URL != "" {
		t.Errorf("feuille cash = %+v", last)
	}
	// petits-enfants : les actifs
	if pea.Children[1].Children[0].URL != "/asset/cw8" {
		t.Errorf("feuille actif = %+v", pea.Children[1].Children[0])
	}
}

func TestBuildTreeByGroup(t *testing.T) {
	tree := buildTree(sampleLines(t), "groupe")
	// racines : immo (450000), actions (9840), liquidités (4050)
	if len(tree) != 3 || tree[1].Label != "actions" {
		t.Fatalf("racines = %+v", tree)
	}
	actions := tree[1]
	if actions.URL != "/group/actions" || actions.Gross != 9840 {
		t.Errorf("actions = %+v", actions)
	}
	// enfants : enveloppes, lien intersection
	if len(actions.Children) != 2 || actions.Children[0].URL != "/account/pea/group/actions" {
		t.Fatalf("enfants actions = %+v", actions.Children)
	}
	// liquidités : enfants = enveloppes non cliquables au 3e niveau
	liq := tree[2]
	if liq.URL != "" || len(liq.Children) != 1 || liq.Children[0].Label != "PEA" {
		t.Errorf("liquidités = %+v", liq)
	}
}

func TestFlatAssets(t *testing.T) {
	flat := flatAssets(sampleLines(t))
	// maison, cw8 (6720+1120 agrégé), apple — le cash n'y figure pas
	if len(flat) != 3 || flat[0].Label != "Maison" {
		t.Fatalf("flat = %+v", flat)
	}
	if flat[1].Label != "CW8" || flat[1].Gross != 7840 || flat[1].URL != "/asset/cw8" {
		t.Errorf("agrégat cw8 = %+v", flat[1])
	}
}
```

Ajouter à `internal/web/server_test.go` :

```go
func TestDashboardTreeModes(t *testing.T) {
	srv, _ := testServer(t)
	// mode groupe (défaut) : arbre avec details et lien d'intersection
	_, body := get(t, srv, "/")
	for _, want := range []string{"<details", "/account/pea/group/actions", "par actif"} {
		if !strings.Contains(body, want) {
			t.Errorf("mode groupe: %q manquant", want)
		}
	}
	// mode actif : liste plate, lien direct actif, pas de details
	_, body = get(t, srv, "/?par=actif")
	if !strings.Contains(body, "/asset/cw8") {
		t.Errorf("mode actif: lien actif manquant:\n%s", excerpt(body))
	}
	// l'onglet courant n'est pas un lien
	if !strings.Contains(body, `actif-onglet`) {
		t.Errorf("onglet actif non marqué")
	}
}
```

(le test existant TestDashboardByEnvelope reste vert : le mode enveloppe garde le
lien « par groupe » et un lien `/account/`.)

- [ ] **Step 2: vérifier l'échec** — FAIL (buildTree inconnu).

- [ ] **Step 3: implémenter**

`internal/web/tree.go` :

```go
package web

import (
	"net/url"
	"slices"
	"strings"

	"finador/internal/portfolio"
)

// node is one displayable level of the répartition tree.
type node struct {
	Label    string
	URL      string // vide : non cliquable
	Gross    float64
	Children []node
}

// buildTree shapes position lines into the two-then-three-level tree:
// mode "enveloppe": account → top-group → assets (cash = leaf « liquidités »);
// mode "groupe": top-group → account (intersection link) → assets, with a
// « liquidités » root whose children are envelopes. Every level sorts by
// descending value.
func buildTree(lines []portfolio.PositionLine, mode string) []node {
	type key2 struct{ a, b string }
	if mode == "enveloppe" {
		// account → group → assets
		byAcc := map[string]*node{}
		grp := map[key2]*node{}
		var accOrder []string
		for _, l := range lines {
			accID := string(l.Account.ID)
			root, ok := byAcc[accID]
			if !ok {
				root = &node{Label: l.Account.Name, URL: "/account/" + url.PathEscape(accID)}
				byAcc[accID] = root
				accOrder = append(accOrder, accID)
			}
			root.Gross += l.Gross
			if l.Asset == nil {
				continue // le cash s'ajoute en feuille à la fin
			}
			g := topGroup(l.Asset.Group)
			k := key2{accID, g}
			child, ok := grp[k]
			if !ok {
				child = &node{Label: g, URL: "/account/" + url.PathEscape(accID) + "/group/" + g}
				grp[k] = child
			}
			child.Gross += l.Gross
			child.Children = append(child.Children, node{
				Label: l.Asset.Name, URL: "/asset/" + url.PathEscape(string(l.Asset.ID)), Gross: l.Gross,
			})
		}
		var out []node
		for _, accID := range accOrder {
			root := byAcc[accID]
			cash := 0.0
			for _, l := range lines {
				if string(l.Account.ID) == accID && l.Asset == nil {
					cash += l.Gross
				}
			}
			for k, child := range grp {
				if k.a != accID {
					continue
				}
				sortNodes(child.Children)
				root.Children = append(root.Children, *child)
			}
			sortNodes(root.Children)
			if cash != 0 {
				root.Children = append(root.Children, node{Label: "liquidités", Gross: cash})
			}
			out = append(out, *root)
		}
		sortNodes(out)
		return out
	}

	// mode "groupe" : top-group → account → assets ; cash → racine liquidités
	byGrp := map[string]*node{}
	sub := map[key2]*node{}
	cashRoot := node{Label: "liquidités"}
	cashByAcc := map[string]*node{}
	for _, l := range lines {
		if l.Asset == nil {
			cashRoot.Gross += l.Gross
			accID := string(l.Account.ID)
			c, ok := cashByAcc[accID]
			if !ok {
				c = &node{Label: l.Account.Name}
				cashByAcc[accID] = c
			}
			c.Gross += l.Gross
			continue
		}
		g := topGroup(l.Asset.Group)
		root, ok := byGrp[g]
		if !ok {
			root = &node{Label: g, URL: "/group/" + g}
			byGrp[g] = root
		}
		root.Gross += l.Gross
		accID := string(l.Account.ID)
		k := key2{g, accID}
		child, ok := sub[k]
		if !ok {
			child = &node{Label: l.Account.Name, URL: "/account/" + url.PathEscape(accID) + "/group/" + g}
			sub[k] = child
		}
		child.Gross += l.Gross
		child.Children = append(child.Children, node{
			Label: l.Asset.Name, URL: "/asset/" + url.PathEscape(string(l.Asset.ID)), Gross: l.Gross,
		})
	}
	var out []node
	for g, root := range byGrp {
		for k, child := range sub {
			if k.a != g {
				continue
			}
			sortNodes(child.Children)
			root.Children = append(root.Children, *child)
		}
		sortNodes(root.Children)
		out = append(out, *root)
	}
	if cashRoot.Gross != 0 {
		for _, c := range cashByAcc {
			cashRoot.Children = append(cashRoot.Children, *c)
		}
		sortNodes(cashRoot.Children)
		out = append(out, cashRoot)
	}
	sortNodes(out)
	return out
}

// flatAssets aggregates each asset across envelopes (cash excluded), sorted
// by descending value.
func flatAssets(lines []portfolio.PositionLine) []node {
	byAsset := map[string]*node{}
	for _, l := range lines {
		if l.Asset == nil {
			continue
		}
		id := string(l.Asset.ID)
		n, ok := byAsset[id]
		if !ok {
			n = &node{Label: l.Asset.Name, URL: "/asset/" + url.PathEscape(id)}
			byAsset[id] = n
		}
		n.Gross += l.Gross
	}
	var out []node
	for _, n := range byAsset {
		out = append(out, *n)
	}
	sortNodes(out)
	return out
}

func topGroup(g string) string {
	if g == "" {
		return "(sans groupe)"
	}
	head, _, _ := strings.Cut(strings.ToLower(g), "/")
	return head
}

func sortNodes(ns []node) {
	slices.SortStableFunc(ns, func(a, b node) int {
		switch {
		case a.Gross > b.Gross:
			return -1
		case a.Gross < b.Gross:
			return 1
		}
		return strings.Compare(a.Label, b.Label)
	})
}
```

NOTE : « liquidités » non cliquable au 1er niveau du mode groupe et en feuille du
mode enveloppe (URL vide) — assumé : le cash n'a pas de page de détail propre.

`internal/web/handlers.go` — le dashboard remplace Parts par les arbres :

```go
type dashData struct {
	Aujourdhui domain.Date
	Val        portfolio.Valuation
	Curve      template.HTML
	Rows       []perf.Row
	Met        perf.Metrics
	Mode       string // groupe | enveloppe | actif
	Onglets    []onglet
	Tree       []node
	Flat       []node
	Warnings   []string
	Flash      string
	Erreur     string
}

type onglet struct {
	Label, URL string
	Actif      bool
}
```

Dans le handler : `mode := r.URL.Query().Get("par")` normalisé
(`enveloppe`/`actif`, défaut `groupe`) ; `lines, err := portfolio.Breakdown(b, today, ccy, fx)`
(erreur → renderError) ; `data.Tree = buildTree(lines, mode)` ou
`data.Flat = flatAssets(lines)` pour `actif` ; Onglets construits avec l'actif
marqué. SUPPRIMER l'ancien WithLinesByAccount du handler web et la construction de
Parts (la CLI garde --by ; vérifier que plus rien ne référence `part`).

`dashboard.html` — la section répartition devient :

```html
  <div>
    <h2>répartition</h2>
    <div class="onglets">
      {{range .Onglets}}{{if .Actif}}<span class="actif-onglet">{{.Label}}</span>{{else}}<a href="{{.URL}}">{{.Label}}</a>{{end}}{{end}}
    </div>
    {{if eq .Mode "actif"}}
    <ul class="arbre">
      {{range .Flat}}
      <li class="feuille"><a href="{{.URL}}">{{.Label}}</a><span class="leader"></span><span class="montant">{{frMoney .Gross $.Val.Currency}}</span></li>
      {{end}}
    </ul>
    {{else}}
    <ul class="arbre">
      {{range .Tree}}
      <li><details>
        <summary>{{if .URL}}<a href="{{.URL}}">{{.Label}}</a>{{else}}<span>{{.Label}}</span>{{end}}<span class="leader"></span><span class="montant">{{frMoney .Gross $.Val.Currency}}</span></summary>
        <ul>
          {{range .Children}}
          <li>{{if .Children}}<details>
            <summary>{{if .URL}}<a href="{{.URL}}">{{.Label}}</a>{{else}}<span>{{.Label}}</span>{{end}}<span class="leader"></span><span class="montant">{{frMoney .Gross $.Val.Currency}}</span></summary>
            <ul>
              {{range .Children}}
              <li class="feuille">{{if .URL}}<a href="{{.URL}}">{{.Label}}</a>{{else}}<span>{{.Label}}</span>{{end}}<span class="leader"></span><span class="montant">{{frMoney .Gross $.Val.Currency}}</span></li>
              {{end}}
            </ul>
          </details>{{else}}<div class="feuille">{{if .URL}}<a href="{{.URL}}">{{.Label}}</a>{{else}}<span>{{.Label}}</span>{{end}}<span class="leader"></span><span class="montant">{{frMoney .Gross $.Val.Currency}}</span></div>{{end}}</li>
          {{end}}
        </ul>
      </details></li>
      {{end}}
    </ul>
    {{end}}
  </div>
```

Onglets : `[]onglet{{"par groupe", "/", mode=="groupe"}, {"par enveloppe", "/?par=enveloppe", mode=="enveloppe"}, {"par actif", "/?par=actif", mode=="actif"}}`.

- [ ] **Step 4: vérifier** — `go test ./internal/web/ -count=1 && go test ./... -count=1`
  vert (dont TestDashboardByEnvelope existant). Lancer le vrai serveur 3 s et vérifier
  visuellement au curl la structure details imbriquée des trois modes.

- [ ] **Step 5: commit** — `git add internal/web && git commit -m "feat(web): répartition arborescente — groupe/enveloppe/actif, niveaux croisés cliquables"`

---

### Task F5: web — édition de transactions + finition v0.3

**Files:**
- Create: `internal/web/templates/tx-edit.html`
- Modify: `internal/web/tx.go`, `internal/web/server.go` (routes), `internal/web/templates/tx.html` (lien édit.)
- Modify: `README.md` (3 lignes v0.3), `docs/superpowers/DECISIONS.md` (D11)
- Test: `internal/web/server_test.go` (ajout)

Sémantique : `GET /tx/{id}/edit` affiche le bordereau prérempli ; `POST /tx/{id}/edit`
revalide TOUT comme une création (parseTxForm), copie les champs sur la transaction
existante en CONSERVANT ID et ImportHash, sauve, 303 → /tx. Validation en échec →
400 avec le bordereau ré-affiché et le message. ID inconnu → 404.

- [ ] **Step 1: tests qui échouent**

```go
func TestTxEditWeb(t *testing.T) {
	srv, f := testServer(t)
	id := f.Book.Transactions[1].ID // le buy cw8

	code, body := get(t, srv, fmt.Sprintf("/tx/%d/edit", id))
	if code != http.StatusOK {
		t.Fatalf("GET edit = %d", code)
	}
	for _, want := range []string{`value="2026-06-01"`, `value="10"`, "Amundi MSCI World", "bordereau"} {
		if !strings.Contains(body, want) {
			t.Errorf("formulaire d'édition: %q manquant", want)
		}
	}

	// modification quantité + montant
	code, body, loc := postForm(t, srv, fmt.Sprintf("/tx/%d/edit", id), url.Values{
		"date": {"2026-06-01"}, "kind": {"buy"}, "account": {"pea"}, "asset": {"cw8"},
		"qty": {"12"}, "amount": {"6600"}, "note": {"corrigé via web"},
	})
	if code != http.StatusSeeOther || loc != "/tx" {
		t.Fatalf("POST edit = %d → %q\n%s", code, loc, excerpt(body))
	}
	tx, err := f.Book.Tx(id)
	if err != nil || tx.Quantity.String() != "12" || tx.Amount.Amount.String() != "6600" || tx.Note != "corrigé via web" {
		t.Fatalf("tx après édition: %+v, %v", tx, err)
	}

	// validation en échec → 400, rien modifié
	code, body, _ = postForm(t, srv, fmt.Sprintf("/tx/%d/edit", id), url.Values{
		"date": {"pas-une-date"}, "kind": {"buy"}, "account": {"pea"}, "asset": {"cw8"},
		"qty": {"12"}, "amount": {"6600"},
	})
	if code != http.StatusBadRequest {
		t.Fatalf("POST edit invalide = %d", code)
	}
	// id inconnu → 404
	if code, _ := get(t, srv, "/tx/999/edit"); code != http.StatusNotFound {
		t.Errorf("GET edit inconnu = %d", code)
	}
	// la liste /tx porte le lien d'édition
	if _, body := get(t, srv, "/tx"); !strings.Contains(body, fmt.Sprintf("/tx/%d/edit", id)) {
		t.Errorf("lien édit. absent de /tx")
	}
}
```

- [ ] **Step 2: vérifier l'échec** — FAIL (404).

- [ ] **Step 3: implémenter**

Routes :

```go
	mux.HandleFunc("GET /tx/{id}/edit", s.txEditPage)
	mux.HandleFunc("POST /tx/{id}/edit", s.txEditSubmit)
```

`internal/web/tx.go` :

```go
type txEditData struct {
	Aujourdhui domain.Date
	Tx         *domain.Transaction
	AssetName  string
	Accounts   []*domain.Account
	Assets     []*domain.Asset
	Kinds      []string
	Erreur     string
}

func (s *Server) findTx(w http.ResponseWriter, r *http.Request) (*domain.Transaction, bool) {
	id, err := strconv.ParseUint(r.PathValue("id"), 10, 64)
	if err != nil {
		s.renderError(w, http.StatusBadRequest, "identifiant invalide")
		return nil, false
	}
	tx, err := s.file.Book.Tx(domain.TxID(id))
	if err != nil {
		s.renderError(w, http.StatusNotFound, "transaction introuvable")
		return nil, false
	}
	return tx, true
}

func (s *Server) txEditPage(w http.ResponseWriter, r *http.Request) {
	s.mu.RLock()
	defer s.mu.RUnlock()
	tx, ok := s.findTx(w, r)
	if !ok {
		return
	}
	s.renderTxEdit(w, http.StatusOK, tx, "")
}

// renderTxEdit est appelé verrou déjà pris.
func (s *Server) renderTxEdit(w http.ResponseWriter, status int, tx *domain.Transaction, erreur string) {
	b := s.file.Book
	data := txEditData{
		Aujourdhui: domain.Today(),
		Tx:         tx,
		Accounts:   b.Accounts,
		Assets:     b.Assets,
		Kinds:      []string{"buy", "sell", "deposit", "withdraw", "dividend", "fee", "statement"},
		Erreur:     erreur,
	}
	s.render(w, status, "tx-edit.html", data)
}

func (s *Server) txEditSubmit(w http.ResponseWriter, r *http.Request) {
	s.mu.Lock()
	defer s.mu.Unlock()
	tx, ok := s.findTx(w, r)
	if !ok {
		return
	}
	parsed, err := parseTxForm(s.file.Book, r)
	if err != nil {
		s.renderTxEdit(w, http.StatusBadRequest, tx, err.Error())
		return
	}
	// conserver l'identité et l'empreinte d'import (une ligne éditée ne doit
	// pas réapparaître au prochain ré-import)
	parsed.ID, parsed.ImportHash = tx.ID, tx.ImportHash
	*tx = parsed
	if err := s.file.Save(); err != nil {
		s.renderTxEdit(w, http.StatusInternalServerError, tx, "sauvegarde impossible : "+err.Error())
		return
	}
	http.Redirect(w, r, "/tx", http.StatusSeeOther)
}
```

(`txDelete` peut désormais réutiliser findTx — refactor optionnel, ne pas casser ses
tests.)

`internal/web/templates/tx-edit.html` :

```html
{{define "tx-edit.html"}}{{template "base" .}}{{end}}
{{define "title"}}édition — finador{{end}}
{{define "main"}}
<p class="fil-ariane"><a href="/tx">transactions</a> → édition [{{.Tx.ID}}]</p>
<section>
  <h2>corriger l'écriture [{{.Tx.ID}}]</h2>
  {{if .Erreur}}<p class="flash erreur">{{.Erreur}}</p>{{end}}
  <form method="post" action="/tx/{{.Tx.ID}}/edit">
    <fieldset>
      <legend>bordereau</legend>
      <div class="champs">
        <div><label for="date">date</label><input id="date" name="date" value="{{.Tx.Date}}"></div>
        <div><label for="kind">type</label>
          <select id="kind" name="kind">{{$k := .Tx.Kind.String}}{{range .Kinds}}<option {{if eq . $k}}selected{{end}}>{{.}}</option>{{end}}</select></div>
        <div><label for="account">compte</label>
          <select id="account" name="account">{{$a := .Tx.Account}}{{range .Accounts}}<option value="{{.ID}}" {{if eq .ID $a}}selected{{end}}>{{.Name}}</option>{{end}}</select></div>
        <div><label for="asset">actif (optionnel)</label>
          <select id="asset" name="asset">{{$s := .Tx.Asset}}<option value=""></option>{{range .Assets}}<option value="{{.ID}}" {{if eq .ID $s}}selected{{end}}>{{.Name}}</option>{{end}}</select></div>
        <div><label for="qty">quantité</label><input id="qty" name="qty" value="{{.Tx.Quantity}}"></div>
        <div><label for="amount">montant total</label><input id="amount" name="amount" value="{{.Tx.Amount.Amount}}"></div>
        <div><label for="ccy">devise</label><input id="ccy" name="ccy" value="{{.Tx.Amount.Currency}}"></div>
        <div><label for="note">note</label><input id="note" name="note" value="{{.Tx.Note}}"></div>
      </div>
      <button type="submit">Enregistrer la correction</button>
    </fieldset>
  </form>
</section>
{{end}}
```

`tx.html` : dans la colonne actions de chaque ligne, AVANT le bouton suppr. :
`<a href="/tx/{{.ID}}/edit">édit.</a> `.

- [ ] **Step 4: finition de phase**

- README : 3 lignes dans la section v0.2 renommée « Nouveautés v0.2/v0.3 » (édition
  des transactions au web, répartition à 3 modes avec arborescence dépliable et
  niveaux croisés cliquables, liens au thème).
- DECISIONS.md D11 :

```markdown
## D11 — Arborescence web en details/summary natifs, portée d'intersection

**Contexte :** la répartition demandait des niveaux dépliables et des niveaux croisés
cliquables (enveloppe ∩ groupe) sans JavaScript. **Choix :** <details>/<summary>
natifs stylés au thème ; nouvelle portée ByAccountGroup (URL canonique
/account/x/group/y — un motif Go ne supporte pas un segment multiple au milieu, pas
de forme réciproque) ; agrégation des groupes au premier segment dans les arbres ;
« liquidités » non cliquable (pas de page propre). **Alternative si refusé :**
arbre toujours déplié, ou page dédiée au cash par enveloppe.
```

- Portillons : `gofmt -l . && go vet ./... && go test ./... -count=1` puis
  `go test -race ./internal/web/ -count=1`.
- Smoke : serveur réel 5 s sur un livre démo offline → curl des trois modes de
  répartition, d'une vue croisée, du formulaire d'édition (GET + POST) ; SIGTERM
  propre ; coller la sortie.

- [ ] **Step 5: commit + tag**

```bash
git add internal/web README.md docs/superpowers/DECISIONS.md
git commit -m "feat(web): édition des transactions — la v0.3 est complète"
git tag phase-f
```


