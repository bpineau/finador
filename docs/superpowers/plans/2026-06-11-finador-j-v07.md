# Finador phase J - v0.7

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Retours utilisateur : (1) supprimer les traits horizontaux des graphes
(garder les étiquettes d'échelle) ; (2) retirer l'onglet « by asset » de la section
allocation (redondant avec l'onglet Assets) ; (3) alias de COMPTES + commande
`account edit` (les actifs ont déjà tout : casse, ISIN, alias, préfixes) ;
(4) formulaire de transaction web : `<datalist>` au lieu de `<select>` pour compte
et actif - texte libre AVEC suggestions, création à la volée des inconnus (comme
l'import CSV) ; fix STRUCTUREL de l'alignement des champs (libellés nowrap, mentions
« optional » en placeholder) ; (5) un camembert (donut) d'allocation par groupe en
bas de l'Overview.

**Conventions inchangées** (TDD, gofmt/vet, anglais, zéro JS, RLock/Lock, pas de
binaire, tests sans réseau).

**Décisions actées (D14 à journaliser en J4) :**
- Création à la volée web : un compte inconnu naît EUR/`none`, un actif inconnu naît
  security/ticker=saisie (mêmes règles que l'import CSV). Un typo crée donc une
  entité - assumé (l'import fait pareil), `asset rm`/`tx edit` réparent.
- Camembert : donut SVG serveur, parts = groupes de tête + cash (mêmes racines que
  l'arbre), palette feutrée assortie au thème, légende HTML à droite (pastille,
  libellé, %, montant). Sections property INCLUSES (c'est du poids d'allocation).
- Onglet « by asset » : retiré des onglets ; `?by=asset` redevient `group`
  silencieusement ; `flatAssets` et son test sont SUPPRIMÉS (code mort).

---

### Task J1: chart - grilles supprimées, renderer Pie

**Files:**
- Modify: `internal/chart/svg.go` (suppression des `<line>` de grille, étiquettes conservées)
- Create: `internal/chart/pie.go`
- Test: `internal/chart/pie_test.go`, ajuster svg_test.go si besoin

- [ ] **Step 1: tests qui échouent**

`internal/chart/pie_test.go`:

```go
package chart

import (
	"strings"
	"testing"
)

func TestPie(t *testing.T) {
	out := Pie([]float64{50, 30, 20}, []string{"#1c1914", "#1e6e4e", "#9a6b1f"}, 180)
	if strings.Count(out, "<path") != 3 {
		t.Fatalf("paths = %d, want 3:\n%s", strings.Count(out, "<path"), out)
	}
	for _, want := range []string{`viewBox="0 0 180 180"`, `fill="#1e6e4e"`, "</svg>"} {
		if !strings.Contains(out, want) {
			t.Errorf("%q missing", want)
		}
	}
	for _, bad := range []string{"NaN", "Inf", "<text"} {
		if strings.Contains(out, bad) {
			t.Errorf("%s should not appear", bad)
		}
	}
}

func TestPieSingleAndEmpty(t *testing.T) {
	if out := Pie(nil, nil, 180); out != "" {
		t.Errorf("empty: %q", out)
	}
	if out := Pie([]float64{0, 0}, []string{"#000", "#000"}, 180); out != "" {
		t.Errorf("all-zero: %q", out)
	}
	// une seule part non nulle → un anneau complet (deux arcs, pas un path dégénéré)
	out := Pie([]float64{0, 42}, []string{"#000", "#1c1914"}, 180)
	if !strings.Contains(out, "<path") && !strings.Contains(out, "<circle") {
		t.Errorf("single slice should render: %q", out)
	}
}

func TestPieDeterministic(t *testing.T) {
	a := Pie([]float64{3, 2, 1}, []string{"#a", "#b", "#c"}, 120)
	b := Pie([]float64{3, 2, 1}, []string{"#a", "#b", "#c"}, 120)
	if a != b {
		t.Error("non deterministic")
	}
}

func TestNoGridLines(t *testing.T) {
	out := SVG([]Line{{Label: "g", Color: "#1c1914", Points: ramp(30)}}, 800, 300)
	if strings.Contains(out, "<line") {
		t.Error("grid lines should be gone")
	}
	// les étiquettes d'échelle restent
	if !strings.Contains(out, "129") && !strings.Contains(out, "100") {
		t.Error("scale labels should remain")
	}
}
```

- [ ] **Step 2: échec vérifié.** **Step 3: implémenter.**

svg.go : supprimer le `fmt.Fprintf(&b, `+"`<line …`"+`…)` de la boucle de grille,
GARDER le `<text>` d'échelle (vérifier que svg_test.go ne casse pas - il n'asserte
pas les lignes).

`internal/chart/pie.go`:

```go
package chart

import (
	"fmt"
	"math"
	"strings"
)

// PiePalette is the muted, paper-friendly palette of the allocation donut.
// Handlers cycle through it and reuse the same colors in the HTML legend.
var PiePalette = []string{
	"#1c1914", "#1e6e4e", "#9a6b1f", "#a3332e",
	"#48657a", "#7d5a4f", "#5d564a", "#8a8466",
}

// Pie renders a donut: one path per positive value, colors supplied by the
// caller (cycled by the handler from PiePalette). No labels - the legend is
// HTML. Returns "" when nothing is positive.
func Pie(values []float64, colors []string, size int) string {
	total := 0.0
	for _, v := range values {
		if v > 0 {
			total += v
		}
	}
	if total == 0 {
		return ""
	}
	cx, cy := float64(size)/2, float64(size)/2
	rOut := float64(size)/2 - 2
	rIn := rOut * 0.55

	var b strings.Builder
	fmt.Fprintf(&b, `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 %d %d" width="%d" height="%d">`, size, size, size, size)
	angle := -math.Pi / 2 // départ à midi
	for i, v := range values {
		if v <= 0 {
			continue
		}
		frac := v / total
		color := "#1c1914"
		if i < len(colors) {
			color = colors[i]
		}
		if frac >= 0.9999 { // anneau complet : deux demi-anneaux pour éviter l'arc dégénéré
			fmt.Fprintf(&b, `<path d="%s" fill="%s"/>`, ringHalf(cx, cy, rOut, rIn, angle, angle+math.Pi), color)
			fmt.Fprintf(&b, `<path d="%s" fill="%s"/>`, ringHalf(cx, cy, rOut, rIn, angle+math.Pi, angle+2*math.Pi), color)
			break
		}
		a1 := angle + frac*2*math.Pi
		fmt.Fprintf(&b, `<path d="%s" fill="%s"/>`, ringHalf(cx, cy, rOut, rIn, angle, a1), color)
		angle = a1
	}
	b.WriteString("</svg>")
	return b.String()
}

// ringHalf builds one donut sector path between two angles.
func ringHalf(cx, cy, rOut, rIn, a0, a1 float64) string {
	laf := 0
	if a1-a0 > math.Pi {
		laf = 1
	}
	f := func(v float64) string { return fmt.Sprintf("%.2f", v) }
	x0o, y0o := cx+rOut*math.Cos(a0), cy+rOut*math.Sin(a0)
	x1o, y1o := cx+rOut*math.Cos(a1), cy+rOut*math.Sin(a1)
	x1i, y1i := cx+rIn*math.Cos(a1), cy+rIn*math.Sin(a1)
	x0i, y0i := cx+rIn*math.Cos(a0), cy+rIn*math.Sin(a0)
	return "M" + f(x0o) + "," + f(y0o) +
		" A" + f(rOut) + "," + f(rOut) + " 0 " + fmt.Sprint(laf) + " 1 " + f(x1o) + "," + f(y1o) +
		" L" + f(x1i) + "," + f(y1i) +
		" A" + f(rIn) + "," + f(rIn) + " 0 " + fmt.Sprint(laf) + " 0 " + f(x0i) + "," + f(y0i) + " Z"
}
```

- [ ] **Step 4:** `go test ./... -count=1` vert. **Step 5:** commit
  `feat(chart): drop chart grid lines, add allocation donut`

---

### Task J2: domain+cli - alias de comptes et account edit

**Files:**
- Modify: `internal/domain/account.go` (champ Aliases), `internal/domain/book.go`
  (tier alias dans Account(), CheckAccountRefs exact, AddAccount l'utilise),
  `internal/cli/account.go` (account edit + colonne ALIASES dans list)
- Test: book_test.go, cli_test.go (ajouts)

Sémantique :
- `Account.Aliases []string json:"aliases,omitempty"`.
- Résolution compte : tiers ID → alias → nom (l'alias est plus « volontaire » que le
  nom, il prime sur lui) - tous insensibles à la casse, préfixe unique ensuite
  (mécanique existante de resolve, ajouter simplement le tier).
- `CheckAccountRefs(a *Account)` : collision EXACTE (EqualFold) de {ID, Name,
  Aliases...} contre les autres comptes - remplace la boucle inline d'AddAccount
  et sert à `account edit`.
- `finador account edit <ref> [--name n] [--tax r] [--ccy c] [--add-alias a]...
  [--rm-alias a]...` - mêmes patterns qu'asset edit (mutation puis CheckAccountRefs,
  mutate() jette en cas d'erreur). `account list` gagne une colonne ALIASES.

- [ ] **Step 1: tests qui échouent**

book_test.go :

```go
func TestAccountAliases(t *testing.T) {
	b := sampleBook(t)
	pea, _ := b.Account("pea-bforbank")
	pea.Aliases = []string{"pea", "bfb"}
	if err := b.CheckAccountRefs(pea); err != nil {
		t.Fatal(err)
	}
	for _, ref := range []string{"PEA", "bfb", "BFB"} {
		if acc, err := b.Account(ref); err != nil || acc.ID != "pea-bforbank" {
			t.Errorf("Account(%q) = %v, %v", ref, acc, err)
		}
	}
	// collision d'alias avec un nom existant → ErrDuplicate
	if err := b.AddAccount(&Account{ID: "autre", Name: "Autre", Currency: EUR}); err != nil {
		t.Fatal(err)
	}
	autre, _ := b.Account("autre")
	autre.Aliases = []string{"PEA BforBank"}
	if err := b.CheckAccountRefs(autre); !errors.Is(err, ErrDuplicate) {
		t.Errorf("collision = %v", err)
	}
}
```

cli_test.go :

```go
func TestAccountEdit(t *testing.T) {
	db := newDB(t)
	run(t, db, "account", "add", "PEA BforBank", "--tax", "gains:17.2%")
	run(t, db, "account", "add", "Savings")
	run(t, db, "account", "edit", "pea-bforbank", "--add-alias", "p")
	out := run(t, db, "account", "list")
	if !strings.Contains(out, "ALIASES") || !strings.Contains(out, "p") {
		t.Errorf("aliases column:\n%s", out)
	}
	// l'alias résout pour la saisie
	run(t, db, "asset", "add", "CW8.PA", "--id", "cw8")
	run(t, db, "deposit", "p", "1000", "2026-01-10")
	if out := run(t, db, "tx", "list", "--account", "p"); !strings.Contains(out, "deposit") {
		t.Errorf("alias should resolve in tx list:\n%s", out)
	}
	// collision refusée, --rm-alias marche, edit du taux marche
	if _, err := tryRun(t, db, "account", "edit", "Savings", "--add-alias", "PEA BforBank"); err == nil {
		t.Fatal("alias collision accepted")
	}
	run(t, db, "account", "edit", "pea-bforbank", "--rm-alias", "p", "--tax", "gains:30%")
	if out := run(t, db, "account", "list"); !strings.Contains(out, "gains:30%") {
		t.Errorf("tax edit:\n%s", out)
	}
}
```

- [ ] **Step 2: échec.** **Step 3: implémenter** (suivre asset edit comme modèle ;
  le tier alias s'insère dans Book.Account() entre ID et Name ; AddAccount délègue
  à CheckAccountRefs). **Step 4:** tout vert. **Step 5:** commit
  `feat: account aliases and account edit`

---

### Task J3: web - datalists avec création à la volée, alignement, onglets

**Files:**
- Modify: `internal/portfolio/import.go` (exporter EnsureAccount/EnsureAsset -
  wrappers fins sur ensureAccount/ensureAsset), `internal/web/tx.go` (parseTxForm
  crée à la volée), `internal/web/templates/tx.html` + `tx-edit.html` (datalist),
  `internal/web/static/style.css` (labels nowrap), `internal/web/handlers.go` +
  `tree.go` + `templates/dashboard.html` (retrait by-asset, flatAssets supprimé)
- Test: server_test.go, tree_test.go (retrait TestFlatAssets), import_test.go

Détails :
1. portfolio : `func EnsureAccount(b *domain.Book, ref, ccy string) (*domain.Account, error)`
   et `EnsureAsset(b *domain.Book, ref string, ccy domain.Currency, group string)
   (*domain.Asset, error)` - exposent les helpers d'import existants (création
   uniquement sur ErrNotFound, ambiguïté propagée).
2. web parseTxForm : compte - `b.Account(ref)` ; si ErrNotFound →
   `portfolio.EnsureAccount(b, ref, "")` (devise EUR par défaut). Actif - idem avec
   `EnsureAsset(b, ref, ccyDuCompte, "")`. Les selects deviennent :

```html
<div><label for="account">account</label>
  <input id="account" name="account" list="accounts-list" placeholder="existing or new">
  <datalist id="accounts-list">{{range .Accounts}}<option value="{{.Name}}">{{end}}</datalist></div>
<div><label for="asset">asset</label>
  <input id="asset" name="asset" list="assets-list" placeholder="optional - existing or new">
  <datalist id="assets-list">{{range .Assets}}<option value="{{.Name}}">{{end}}</datalist></div>
```
   (tx-edit.html : mêmes champs, `value="{{…}}"` prérempli avec le NOM résolu -
   passer AccountName/AssetName dans txEditData.)
3. Alignement structurel : style.css - `label { white-space: nowrap; overflow: hidden;
   text-overflow: ellipsis; }` et `.champs { grid-template-columns:
   repeat(auto-fit, minmax(10.5rem, 1fr)); }` ; dans les templates, supprimer
   « (optional) » des libellés (→ placeholders) : labels `date / kind / account /
   asset / quantity / total amount / currency / note` UNIQUEMENT.
4. Onglets : retirer « by asset » (Onglets = group, account) ; `?by=asset` →
   normalisé `group` ; supprimer `flatAssets` + `Flat` + la branche template +
   `TestFlatAssets` ; adapter TestDashboardTreeModes (l'assertion « by asset »
   disparaît, vérifier qu'il NE reste PAS d'onglet by asset).

Tests web nouveaux :

```go
func TestTxCreateOnTheFly(t *testing.T) {
	srv, f := testServer(t)
	code, _, loc := postForm(t, srv, "/tx", url.Values{
		"date": {"2026-06-03"}, "kind": {"deposit"}, "account": {"Brand New Bank"},
		"amount": {"500"},
	})
	if code != http.StatusSeeOther || loc != "/tx" {
		t.Fatalf("create-on-the-fly = %d → %q", code, loc)
	}
	if _, err := f.Book.Account("Brand New Bank"); err != nil {
		t.Fatalf("account not created: %v", err)
	}
	// actif à la volée
	code, _, _ = postForm(t, srv, "/tx", url.Values{
		"date": {"2026-06-03"}, "kind": {"buy"}, "account": {"pea"},
		"asset": {"NVDA"}, "qty": {"2"}, "amount": {"300"},
	})
	if code != http.StatusSeeOther {
		t.Fatalf("asset on the fly = %d", code)
	}
	if a, err := f.Book.Asset("NVDA"); err != nil || a.Kind != domain.Security {
		t.Fatalf("asset not created: %v %v", a, err)
	}
	// le formulaire est en datalist, plus en select
	_, body := get(t, srv, "/tx")
	if !strings.Contains(body, "<datalist") || strings.Contains(body, `<select id="account"`) {
		t.Error("account field should be a datalist input")
	}
}
```

- [ ] **Step 4:** tout vert + -race web ; live check du formulaire.
- [ ] **Step 5:** commit `feat(web): datalist entry with on-the-fly creation, stable form alignment`

---

### Task J4: web - camembert d'allocation + finition

**Files:**
- Modify: `internal/web/handlers.go`, `internal/web/templates/dashboard.html`,
  `internal/web/static/style.css`, README, DECISIONS (D14)
- Test: server_test.go

En bas de l'Overview (après la section performance) : section « weights » - donut
`chart.Pie` (taille 190) des groupes de tête + cash (mêmes racines que l'arbre :
réutiliser buildTree(lines, "group") et prendre les racines OU agréger directement
depuis Breakdown par topGroup + cash), légende à droite : pastille couleur
(PiePalette cyclée), libellé (lien groupe quand applicable), pourcentage, montant.

handler : construire `[]pieSlice{Label, URL, Amount, Percent, Color}` trié par
montant décroissant (couleurs affectées APRÈS tri, dans l'ordre), passer le SVG
(template.HTML - généré par nous) + la légende.

```go
type pieSlice struct {
	Label, URL, Color string
	Amount            float64
	Percent           int
}
```

Template :

```html
<section>
  <h2>weights</h2>
  <div class="camembert">
    <figure>{{.Pie}}</figure>
    <ul class="pie-legend">
      {{range .PieSlices}}
      <li><span class="pastille" style="background: {{.Color}}"></span>
          {{if .URL}}<a href="{{.URL}}">{{.Label}}</a>{{else}}{{.Label}}{{end}}
          <span class="leader"></span>
          <span class="montant">{{.Percent}}% · {{fmtMoney .Amount $.Val.Currency}}</span></li>
      {{end}}
    </ul>
  </div>
</section>
```

NOTE html/template : un style inline avec une couleur dynamique passe par
l'échappement CSS - les couleurs viennent de PiePalette (constantes sûres) ; si le
contexte CSS pose problème (template.CSS requis), passer `Color` en
`template.CSS` dans le handler avec un commentaire expliquant que la palette est
constante.

CSS :

```css
.camembert { display: flex; gap: 2rem; align-items: center; flex-wrap: wrap; }
.camembert figure { margin: 0; }
.pie-legend { list-style: none; margin: 0; padding: 0; flex: 1; min-width: 16rem; font-size: .85rem; }
.pie-legend li { display: flex; align-items: baseline; gap: .55rem; padding: .22rem 0; }
.pie-legend .pastille { width: .65rem; height: .65rem; flex: none; transform: translateY(1px); }
```

Test :

```go
func TestDashboardPie(t *testing.T) {
	srv, _ := testServer(t)
	_, body := get(t, srv, "/")
	for _, want := range []string{"weights", "pie-legend", "pastille", `viewBox="0 0 190 190"`} {
		if !strings.Contains(body, want) {
			t.Errorf("pie: %q missing", want)
		}
	}
}
```

Finition : README (« an allocation donut at the bottom of the overview »), D14
(création à la volée assumée + retrait by-asset + donut), portillons complets,
smoke live (camembert visible sur le demo), tag `phase-j`.
